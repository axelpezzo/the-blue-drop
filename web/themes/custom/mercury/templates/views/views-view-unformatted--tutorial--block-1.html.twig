<div class="container mx-auto px-4 py-16 md:py-20">

  {# Header sezione #}
  <div class="flex flex-col md:flex-row md:items-end justify-between gap-6 mb-12">
    <div class="flex flex-col gap-3">
      <span class="text-xs font-bold uppercase tracking-widest text-primary">Impara con noi</span>
      <h2 class="text-4xl md:text-5xl font-bold tracking-tight leading-none">Tutorial</h2>
      <p class="text-foreground/60 text-lg max-w-xl">Guide pratiche e passo dopo passo per imparare Drupal e le tecnologie correlate.</p>
    </div>
    <a href="/tutorial" class="inline-flex items-center gap-2 text-sm font-semibold text-primary hover:gap-4 transition-all duration-200 shrink-0">
      Tutti i tutorial
      <svg class="w-4 h-4" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
        <path d="M5 12h14M12 5l7 7-7 7"/>
      </svg>
    </a>
  </div>

  {# Griglia tutorial #}
  <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
    {% for row in rows %}
      {{- row.content -}}
    {% endfor %}
  </div>

  {# Footer sezione #}
  <div class="flex justify-center mt-14">
    <a href="/tutorial" class="inline-flex items-center gap-2 rounded-full border-2 border-primary px-8 py-3 text-sm font-semibold text-primary hover:bg-primary hover:text-primary-foreground transition-all duration-200">
      Esplora tutti i tutorial
      <svg class="w-4 h-4" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
        <path d="M5 12h14M12 5l7 7-7 7"/>
      </svg>
    </a>
  </div>

</div>
